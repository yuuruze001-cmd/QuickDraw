<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QuickDraw"
    xmlns:views="using:QuickDraw.Views">

    <Style TargetType="views:SlideTitleBar">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="views:SlideTitleBar">
                    <Grid
                        x:Name="LayoutRoot"
                        Width="Auto"
                        Height="48">
                        <Grid.Resources>

                            <ResourceDictionary>
                                <x:Double x:Key="ProgressBarTrackHeight">0</x:Double>
                                <CornerRadius x:Key="ProgressBarCornerRadius">0</CornerRadius>
                            </ResourceDictionary>

                        </Grid.Resources>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <ProgressBar
                            x:Name="ProgressBar"
                            Grid.Column="0"
                            VerticalAlignment="Bottom"
                            Value="0" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition
                                    x:Name="CenterLeftColumn"
                                    Width="*"
                                    MinWidth="62" />
                                <ColumnDefinition x:Name="CommandsColumn" Width="Auto" />
                                <ColumnDefinition
                                    x:Name="CenterRightColumn"
                                    Width="*"
                                    MinWidth="152" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <StackPanel.Resources>
                                    <StaticResource x:Key="ButtonBackground" ResourceKey="ControlFillColorTransparentBrush" />
                                    <StaticResource x:Key="ButtonBorderBrush" ResourceKey="ControlFillColorTransparentBrush" />
                                </StackPanel.Resources>
                                <Button
                                    x:Name="GrayscaleButton"
                                    Margin="0"
                                    Padding="12,12,12,12"
                                    BorderThickness="0">
                                    <Viewbox Width="16" Height="16">
                                        <FontIcon Glyph="&#xE790;" />
                                    </Viewbox>

                                </Button>
                                <Button
                                    x:Name="PreviousButton"
                                    Margin="10,0,0,0"
                                    Padding="12,12,12,12"
                                    BorderThickness="0">
                                    <Viewbox Width="16" Height="16">
                                        <SymbolIcon Symbol="Previous" />
                                    </Viewbox>
                                </Button>
                                <Button
                                    x:Name="NextButton"
                                    Margin="10,0,0,0"
                                    Padding="12,12,12,12"
                                    BorderThickness="0">
                                    <Viewbox Width="16" Height="16">
                                        <SymbolIcon Symbol="Next" />
                                    </Viewbox>
                                </Button>
                                <Button
                                    x:Name="PauseButton"
                                    Margin="10,0,0,0"
                                    Padding="12,12,12,12"
                                    BorderThickness="0">
                                    <Viewbox Width="16" Height="16">
                                        <SymbolIcon Symbol="Pause" />
                                    </Viewbox>
                                </Button>
                            </StackPanel>
                        </Grid>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition x:Name="LeftInsetColumn" Width="0" />
                                <ColumnDefinition x:Name="BackColumn" Width="Auto" />
                                <ColumnDefinition x:Name="RightInsetColumn" Width="138" />
                            </Grid.ColumnDefinitions>

                            <Button
                                x:Name="BackButton"
                                Grid.Column="1"
                                Margin="0,0,0,0"
                                Padding="14,14,14,14"
                                CornerRadius="0">
                                <FontIcon Glyph="&#xE830;" />
                            </Button>

                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="views:MainTitleBar">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="views:MainTitleBar">
                    <Grid
                        x:Name="LayoutRoot"
                        Width="Auto"
                        Height="32">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition x:Name="LeftInsetColumn" Width="0" />
                            <ColumnDefinition x:Name="TitleColumn" Width="*" />
                            <ColumnDefinition x:Name="RightInsetColumn" Width="138" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="1" Orientation="Horizontal">

                            <Image
                                Width="16"
                                Height="16"
                                Margin="13,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Source="Assets/TitleBar.png"
                                UseLayoutRounding="True" />
                            <TextBlock
                                x:Name="AppTitleTextBlock"
                                Margin="14,0,14,0"
                                VerticalAlignment="Center"
                                Text="QuickDraw"
                                TextWrapping="NoWrap">

                                <TextBlock.Foreground>
                                    <SolidColorBrush x:Name="AppTitleTextBlockForeground" Color="{Binding Color, Source={StaticResource WindowCaptionForeground}}" />
                                </TextBlock.Foreground>
                            </TextBlock>
                        </StackPanel>

                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup>
                                <VisualStateGroup.Transitions>
                                    <VisualTransition From="Active" To="Inactive">
                                        <VisualTransition.GeneratedEasingFunction>
                                            <CubicEase EasingMode="EaseIn" />
                                        </VisualTransition.GeneratedEasingFunction>
                                    </VisualTransition>
                                    <VisualTransition From="Inactive" To="Active">
                                        <VisualTransition.GeneratedEasingFunction>
                                            <CubicEase EasingMode="EaseIn" />
                                        </VisualTransition.GeneratedEasingFunction>
                                    </VisualTransition>
                                </VisualStateGroup.Transitions>

                                <VisualState x:Name="Active" />

                                <VisualState x:Name="Inactive">
                                    <Storyboard>
                                        <ColorAnimation
                                            Storyboard.TargetName="AppTitleTextBlockForeground"
                                            Storyboard.TargetProperty="Color"
                                            To="{Binding Color, Source={StaticResource WindowCaptionForegroundDisabled}}"
                                            Duration="{StaticResource ControlFasterAnimationDuration}" />
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>




</ResourceDictionary>
